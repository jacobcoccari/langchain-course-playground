def generate(message):
    "Function uses a conversationchain to generate a response from ChatOpenAI, along with the streamlit memory objects."
    pass


def add_user_message(message):
    "Adds user message to memory in both streamlit and with ChatOpenAI"
    pass


def add_ai_message(message):
    "Adds AI message to memory in both streamlit"
    pass
